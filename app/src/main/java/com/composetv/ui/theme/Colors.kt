package com.composetv.ui.theme

import androidx.compose.ui.graphics.Color

private const val GRAY = 0xFF808080
private const val LIGHT_GRAY = 0xFFEBEBEB
private const val YELLOW = 0xFFFFCA40
private const val BLACK = 0xFF000000
private const val WHITE = 0xFFF8F8F8

val white = Color(WHITE)
val yellow = Color(YELLOW)
val black = Color(BLACK)
val gray = Color(GRAY)
val lightGray = Color(LIGHT_GRAY)
